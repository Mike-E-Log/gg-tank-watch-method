# Normalize all text to LF so the published-blob sha256 is stable across OSes.
# eval-summary.json's sha256 must stay reproducible across OSes, so keep it LF.
* text=auto eol=lf
*.json text eol=lf
*.md   text eol=lf
